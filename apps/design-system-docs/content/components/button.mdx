---
title: Button
description: A clickable element which communicates that users can trigger an action
category: Components
status: stable
links:
  github: agagag
  storybook: fafafa
keywords:
  - button
  - action
  - click
  - cta
  - call to action
  - interactive
lastUpdated: 2025-01-27
---

import { Button } from '@thrive/ui';
import { MdAdd, MdSave, MdEdit, MdArrowForward, MdDownload, MdOpenInNew, MdDelete } from 'react-icons/md';

<Accordions>
  <Accordion title="Overview">
    A Button is an interactive element that triggers an action when clicked. Use buttons for actions that affect the application state or trigger operations.

    **When to Use:**
    - Submitting forms or data
    - Closing modals or dialogs
    - Triggering application operations
    - Confirming or canceling actions

    **When NOT to Use:**
    - For navigation between pages (use links instead)
    - For toggling settings (use switches or checkboxes)
    - For selecting options (use radio buttons or checkboxes)
  </Accordion>
</Accordions>

<Installation />

## Usage

Import the Button component from the package. For examples with icons, you'll also need to import from `react-icons/md`:

```tsx
import { Button } from '@thrive/ui';
import { MdAdd, MdSave, MdEdit, MdArrowForward, MdDownload, MdOpenInNew, MdDelete } from 'react-icons/md';
```

## Examples

### Basic Button

<CodeExample
	title="Default Button"
	description="The simplest form of a button"
	code={`<Button>Click me</Button>`}
	preview={<Button>Click me</Button>}
/>

### Variants

Buttons come in different visual styles to indicate their importance and purpose.

<CodeExample
	title="Button Variants"
	description="Different visual styles for different use cases"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="destructive">Destructive</Button>
  <Button variant="ghost">Ghost</Button>
  <Button variant="ghost-body">Ghost Body</Button>
  <Button variant="ghost-destructive">Ghost Destructive</Button>
  <Button variant="link">Link</Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="primary">Primary</Button>
			<Button variant="secondary">Secondary</Button>
			<Button variant="destructive">Destructive</Button>
			<Button variant="ghost">Ghost</Button>
			<Button variant="ghost-body">Ghost Body</Button>
			<Button variant="ghost-destructive">Ghost Destructive</Button>
			<Button variant="link">Link</Button>
		</div>
	}
/>

### Sizes

Buttons are available in four sizes to fit different contexts and density needs.

<CodeExample
	title="Button Sizes"
	description="Four size options from extra small to large"
	code={`<div className="flex items-center gap-2">
  <Button size="xs">Extra Small</Button>
  <Button size="sm">Small</Button>
  <Button size="md">Medium</Button>
  <Button size="lg">Large</Button>
</div>`}
	preview={
		<div className="flex items-center gap-2">
			<Button size="xs">Extra Small</Button>
			<Button size="sm">Small</Button>
			<Button size="md">Medium</Button>
			<Button size="lg">Large</Button>
		</div>
	}
/>

**Size recommendations:**
- **xs**: Compact spaces, tables, crowded interfaces
- **sm**: Default density interfaces, secondary actions
- **md**: Default size for most primary actions
- **lg**: Prominent calls-to-action, hero sections

### With Icons

Buttons support icons on both the left and right sides, or as standalone icon-only buttons.

**Using leftIcon and rightIcon props:**

<CodeExample
	title="Buttons with Left Icons"
	description="Use the leftIcon prop for icons before text"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="primary" leftIcon={<MdAdd />}>
    Add Item
  </Button>
  <Button leftIcon={<MdSave />}>
    Save Changes
  </Button>
  <Button variant="secondary" leftIcon={<MdEdit />}>
    Edit
  </Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="primary" leftIcon={<MdAdd />}>Add Item</Button>
			<Button variant="primary" leftIcon={<MdAdd />} size='lg'>Add Item</Button>
			<Button variant="primary" leftIcon={<MdAdd />} size='xs'>Add Item</Button>
			<Button variant="primary" leftIcon={<MdAdd />} size='sm'>Add Item</Button>

			<Button leftIcon={<MdSave />}>Save Changes</Button>
			<Button variant="secondary" leftIcon={<MdEdit />}>Edit</Button>
		</div>
	}
/>

**Right side icons:**

<CodeExample
	title="Buttons with Right Icons"
	description="Use the rightIcon prop for icons after text"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="primary" rightIcon={<MdArrowForward />}>
    Continue
  </Button>
  <Button rightIcon={<MdDownload />}>
    Download
  </Button>
  <Button variant="link" rightIcon={<MdOpenInNew />}>
    Open Link
  </Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="primary" rightIcon={<MdArrowForward />}>Continue</Button>
			<Button rightIcon={<MdDownload />}>Download</Button>
			<Button variant="link" rightIcon={<MdOpenInNew />}>Open Link</Button>
		</div>
	}
/>

### Loading State

Buttons can display a loading state to provide feedback during async operations.

<CodeExample
	title="Loading Buttons"
	description="Show loading state during async operations"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="primary" loading disabled>
    Processing...
  </Button>
  <Button variant="primary" loading loadingText="Saving..." disabled>
    Save Changes
  </Button>
  <Button variant="secondary" loading disabled>
    Loading
  </Button>
  <Button variant="destructive" loading loadingText="Deleting..." disabled>
    Delete
  </Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="primary" loading disabled>Processing...</Button>
			<Button variant="primary" loading loadingText="Saving..." disabled>Save Changes</Button>
			<Button variant="secondary" loading disabled>Loading</Button>
			<Button variant="destructive" loading loadingText="Deleting..." disabled>Delete</Button>
		</div>
	}
/>

**Loading state behavior:**
- Automatically disables the button
- Shows a spinning loader icon
- Optionally shows custom `loadingText`
- Hides icons while loading

### Disabled State

<CodeExample
	title="Disabled Buttons"
	description="Buttons that are temporarily unavailable"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="primary" disabled>Primary Disabled</Button>
  <Button variant="secondary" disabled>Secondary Disabled</Button>
  <Button variant="destructive" disabled>Destructive Disabled</Button>
  <Button variant="ghost" disabled>Ghost Disabled</Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="primary" disabled>Primary Disabled</Button>
			<Button variant="secondary" disabled>Secondary Disabled</Button>
			<Button variant="destructive" disabled>Destructive Disabled</Button>
			<Button variant="ghost" disabled>Ghost Disabled</Button>
		</div>
	}
/>

### Destructive Actions

Use the destructive variant for actions that have permanent consequences like deleting data or canceling subscriptions.

<CodeExample
	title="Destructive Actions"
	description="Highlight permanent and dangerous actions"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="destructive">Delete Account</Button>
  <Button variant="destructive" leftIcon={<MdDelete />}>
    Remove Item
  </Button>
  <Button variant="primary">Save Changes</Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="destructive">Delete Account</Button>
			<Button variant="destructive" leftIcon={<MdDelete />}>Remove Item</Button>
			<Button variant="primary">Save Changes</Button>
		</div>
	}
/>

### As a Link (asChild)

You can render a button as a link element using the `asChild` prop, which is useful for semantic HTML.

<CodeExample
	title="Button as Link"
	description="Render button styles on a link element"
	code={`<div className="flex flex-wrap gap-2">
  <Button variant="primary" asChild>
    <a href="/dashboard">Go to Dashboard</a>
  </Button>
  <Button variant="link" asChild>
    <a href="/documentation">Read Docs</a>
  </Button>
</div>`}
	preview={
		<div className="flex flex-wrap gap-2">
			<Button variant="primary" asChild><a href="/dashboard">Go to Dashboard</a></Button>
			<Button variant="link" asChild><a href="/documentation">Read Docs</a></Button>
		</div>
	}
/>

<InfoCallout type="info" title="Semantic HTML">
When using `asChild`, the button style is applied to the child element. This is useful when you need proper `<a>` tags for SEO or when integrating with Next.js Link components.
</InfoCallout>

## Best Practices

<GuidelinesGrid>
	<Guideline type="do" title="Use action-oriented labels">
		Button text should clearly describe the action that will occur. Use verbs: "Save changes", "Delete account", "Add to cart", "Submit form".
	</Guideline>
	
	<Guideline type="do" title="Provide loading feedback">
		Always show loading state during async operations to prevent users from clicking multiple times and to provide clear feedback.
	</Guideline>
	
	<Guideline type="do" title="Use appropriate size for context">
		Match button size to importance and density: xs/sm for tables and toolbars, md for forms, lg for prominent CTAs.
	</Guideline>
	
	<Guideline type="do" title="Choose the right variant">
		Use primary for main actions, secondary/destructive for important but not primary actions, ghost/link for subtle actions, and destructive for dangerous operations.
	</Guideline>
	
	<Guideline type="dont" title="Avoid vague labels">
		Don't use generic text like "Click here", "OK", "Submit". Be specific about what will happen.
	</Guideline>
	
	<Guideline type="dont" title="Don't use multiple primary buttons">
		Only one primary action per interface. Multiple primary buttons create confusion about hierarchy and importance.
	</Guideline>
	
	<Guideline type="dont" title="Don't use buttons for navigation">
		Use `<Link>` or the `asChild` prop when navigating between pages. Save buttons for actions that change state.
	</Guideline>
	
	<Guideline type="dont" title="Don't forget disabled states">
		Disable buttons when actions aren't available. Provide clear feedback about why an action is disabled.
	</Guideline>
</GuidelinesGrid>

## Props

<Props componentName="Button" />

## Accessibility

<InfoCallout type="info" title="Keyboard Navigation">
Buttons can be activated using the Space or Enter key when focused. Use Tab to navigate between buttons.
</InfoCallout>

<InfoCallout type="warning" title="Button vs Link">
Use a button for actions that affect the current page. Use a link for navigation to different pages or sections.
</InfoCallout>

