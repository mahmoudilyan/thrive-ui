'use client';

import { Box, PageSection, Panel, SideNavigation } from '@thrive/ui';

export default function DashboardPage({ children }: { children: React.ReactNode }) {
	return (
		<>
			<PageSection title="Dashboard" />
		</>
	);
}
